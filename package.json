{
  "name": "soksak-plugin-acp-core",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "soksak ACP 코어 — Agent Client Protocol 클라이언트 엔진(라이브러리 플러그인). 오케스트라·라운지가 의존.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.26.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "esbuild": "^0.24.2",
    "typescript": "~5.9.3",
    "vitest": "^2.1.8"
  }
}
